$(function() {
    $("#login").click(function( event ) {
         alert("Logging you in!");
    })
});

// the login button has an id login which is used here. If the buttin is clicked, an alert is displayed, in that way user knows page is processing.